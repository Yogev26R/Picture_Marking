import { LoginResponseInvalidUserName } from './login-response-invalid-user-name';

describe('LoginResponseInvalidUserName', () => {
  it('should create an instance', () => {
    expect(new LoginResponseInvalidUserName()).toBeTruthy();
  });
});
