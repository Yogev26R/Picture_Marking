import { RegisterResponseUserNameExists } from './register-response-user-name-exists';

describe('RegisterResponseUserNameExists', () => {
  it('should create an instance', () => {
    expect(new RegisterResponseUserNameExists()).toBeTruthy();
  });
});
