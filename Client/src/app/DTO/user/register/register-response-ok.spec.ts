import { RegisterResponseOK } from './register-response-ok';

describe('RegisterResponseOK', () => {
  it('should create an instance', () => {
    expect(new RegisterResponseOK()).toBeTruthy();
  });
});
