import { RegisterResponseEmailAddressExists } from './register-response-email-address-exists';

describe('RegisterResponseEmailAddressExists', () => {
  it('should create an instance', () => {
    expect(new RegisterResponseEmailAddressExists()).toBeTruthy();
  });
});
