import { UnSubscribeRequest } from './un-subscribe-request';

describe('UnSubscribeRequest', () => {
  it('should create an instance', () => {
    expect(new UnSubscribeRequest()).toBeTruthy();
  });
});
