import { UnSubscribeResponseInvalidEmailAddress } from './un-subscribe-response-invalid-email-address';

describe('UnSubscribeResponseInvalidEmailAddress', () => {
  it('should create an instance', () => {
    expect(new UnSubscribeResponseInvalidEmailAddress()).toBeTruthy();
  });
});
