import { UnSubscribeResponse } from './un-subscribe-response';

describe('UnSubscribeResponse', () => {
  it('should create an instance', () => {
    expect(new UnSubscribeResponse()).toBeTruthy();
  });
});
