import { UnSubscribeResponseOK } from './un-subscribe-response-ok';

describe('UnSubscribeResponseOK', () => {
  it('should create an instance', () => {
    expect(new UnSubscribeResponseOK()).toBeTruthy();
  });
});
