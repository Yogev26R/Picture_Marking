import { DeleteDocumentResponse } from './delete-document-response';

describe('DeleteDocumentResponse', () => {
  it('should create an instance', () => {
    expect(new DeleteDocumentResponse()).toBeTruthy();
  });
});
