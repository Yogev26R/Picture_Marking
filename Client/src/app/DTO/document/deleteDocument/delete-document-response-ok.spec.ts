import { DeleteDocumentResponseOK } from './delete-document-response-ok';

describe('DeleteDocumentResponseOK', () => {
  it('should create an instance', () => {
    expect(new DeleteDocumentResponseOK()).toBeTruthy();
  });
});
