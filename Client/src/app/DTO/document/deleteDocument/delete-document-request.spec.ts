import { DeleteDocumentRequest } from './delete-document-request';

describe('DeleteDocumentRequest', () => {
  it('should create an instance', () => {
    expect(new DeleteDocumentRequest()).toBeTruthy();
  });
});
