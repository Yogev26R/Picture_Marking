import { UpdateDocumentResponse } from './update-document-response';

describe('UpdateDocumentResponse', () => {
  it('should create an instance', () => {
    expect(new UpdateDocumentResponse()).toBeTruthy();
  });
});
