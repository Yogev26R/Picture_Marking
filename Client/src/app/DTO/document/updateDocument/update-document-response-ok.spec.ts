import { UpdateDocumentResponseOK } from './update-document-response-ok';

describe('UpdateDocumentResponseOK', () => {
  it('should create an instance', () => {
    expect(new UpdateDocumentResponseOK()).toBeTruthy();
  });
});
