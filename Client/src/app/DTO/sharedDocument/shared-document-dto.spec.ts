import { SharedDocumentDTO } from './shared-document-dto';

describe('SharedDocumentDTO', () => {
  it('should create an instance', () => {
    expect(new SharedDocumentDTO()).toBeTruthy();
  });
});
