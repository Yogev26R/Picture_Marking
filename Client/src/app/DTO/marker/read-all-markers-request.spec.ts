import { ReadAllMarkersRequest } from './read-all-markers-request';

describe('ReadAllMarkersRequest', () => {
  it('should create an instance', () => {
    expect(new ReadAllMarkersRequest()).toBeTruthy();
  });
});
