import { ReadAllMarkersResponse } from './read-all-markers-response';

describe('ReadAllMarkersResponse', () => {
  it('should create an instance', () => {
    expect(new ReadAllMarkersResponse()).toBeTruthy();
  });
});
